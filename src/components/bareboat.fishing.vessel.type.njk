{% macro bareboat_fishing_vessel_type(data, name, classes, jsonPath, id, value, description, labelClass, required) %}
{% set options = [{"name":"Marine Autonomous Surface Ship","value":"Marine Autonomous Surface Ship"}] %}
{% set favourites = undefined %}
{% set favouriteKeys = [] %}
{% from "select.html" import select %}
{{ select(id if id else 'bareboat.fishing.vessel.type', name=name, value=value if value else data.bareboat.fishing.vessel.type, options=options, required=required, description=description, labelClass=labelClass, favourites=favourites, favouriteKeys=favouriteKeys) }}
{% endmacro %}
